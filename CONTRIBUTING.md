# Вклад в Zeelo

Спасибо, что хотите помочь! Ниже руководство, чтобы мы друг другу ничего не ломали.

## 🎯 Первоначальная настройка

### Требования к окружению
- **Node.js**: используйте `nvm use` (версия из `.nvmrc`) или версию 18+
- **Менеджер пакетов**: Yarn v3+
- **Установка**: `yarn install --immutable`

После клонирования репозитория выполните:

```bash
# Установите зависимости
yarn install

# Инициализируйте Git hooks (Husky)
yarn prepare

# Проверьте, что всё работает
yarn check
```

## 📋 Git-flow

- `main` — стабильная продакшн-ветка
- `develop` — интеграционная ветка
- Фичи — только через ветки `feature/<кратко-о-чем>`, багфиксы — `fix/<кратко>`
- Коммиты по Conventional Commits:
  - `feat: …`, `fix: …`, `chore: …`, `refactor: …`, `docs: …`, `style: …`
- PR всегда в `develop`, минимум 1 ревьюер

## 🚀 Локальный запуск

```bash
nvm use                    # или используйте Node.js 18+
yarn install --immutable
yarn dev                   # запуск dev сервера
yarn build                 # проверка сборки
```

## 📝 Workflow

### 1. Создание ветки

```bash
git checkout -b feature/название-функции
# или
git checkout -b fix/описание-бага
```

### 2. Разработка

- Пишите код согласно стандартам проекта
- Регулярно запускайте `yarn check` для проверки
- Коммитьте часто с понятными сообщениями

### 3. Перед коммитом

Git hooks автоматически проверят код, но вы можете запустить вручную:

```bash
# Проверка всего проекта
yarn check

# Или по отдельности
yarn typecheck      # Проверка типов
yarn lint           # Проверка линтером
yarn format:check   # Проверка форматирования
```

### 4. Форматирование

Код автоматически форматируется при коммите, но можно сделать вручную:

```bash
yarn format  # Форматировать все файлы
```

### 5. Коммит

```bash
git add .
git commit -m "feat: добавить новую функцию"
```

**Важно:** Git hooks автоматически проверят и отформатируют измененные файлы перед коммитом.

## 🔍 Проверка кода

### Линтинг

```bash
yarn lint          # Проверка
yarn lint:fix      # Автоисправление
```

### Форматирование

```bash
yarn format        # Форматировать
yarn format:check  # Проверка без изменений
```

### Типы

```bash
yarn typecheck     # Проверка TypeScript
```

### Всё вместе

```bash
yarn check         # Полная проверка (types + lint + format)
```

## 📝 Соглашения о коммитах

Используйте понятные сообщения:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование (не влияет на код)
- `refactor:` - рефакторинг кода
- `test:` - добавление/изменение тестов
- `chore:` - обновление зависимостей, конфигов и т.д.
- `perf:` - улучшение производительности

Примеры:
```
feat: добавить авторизацию через OAuth
fix: исправить баг с отображением списка
refactor: переписать компонент на hooks
docs: обновить README
```

## 🚫 Что не делать

- ❌ Не коммитьте `node_modules/`
- ❌ Не коммитьте `.env` файлы с секретами
- ❌ Не коммитьте собранные файлы (`dist/`, `build/`)
- ❌ Не отключайте Git hooks (`--no-verify`)
- ❌ Не игнорируйте ошибки линтера/типов

## ✅ Чеклист перед PR

- [ ] Код проходит `yarn check`
- [ ] Нет ошибок TypeScript
- [ ] Нет предупреждений линтера
- [ ] Код отформатирован
- [ ] Коммиты имеют понятные сообщения
- [ ] Ветка синхронизирована с `develop`

## 🆘 Проблемы?

### Git hooks не работают

```bash
yarn prepare
```

### Зависимости не устанавливаются

```bash
rm -rf node_modules yarn.lock
yarn install
```

### Ошибки форматирования

```bash
yarn format
```

### Ошибки линтера

```bash
yarn lint:fix
```

## 💡 Советы

1. **Настройте редактор** - включите форматирование при сохранении
2. **Используйте `yarn check`** - перед каждым коммитом
3. **Коммитьте часто** - маленькие коммиты легче ревьюить
4. **Пишите понятные сообщения** - другим разработчикам будет проще понять изменения
