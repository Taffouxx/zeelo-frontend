# Zeelo Frontend

Frontend приложение для Zeelo платформы.

## 🚀 Быстрый старт

### Требования

- **Node.js** версии 18+ (рекомендуется использовать [nvm](https://github.com/nvm-sh/nvm) или [fnm](https://github.com/Schniz/fnm))
- **Yarn** версии 3.2.0 (устанавливается автоматически)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd zeelo-frontend
```

2. Установите зависимости:
```bash
yarn install
```

3. Настройте ассеты (если требуется):
```bash
yarn pull
```

4. Запустите dev сервер:
```bash
yarn dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому порту, указанному Vite).

## 📜 Доступные команды

### Разработка

- `yarn dev` - Запуск dev сервера с hot reload
- `yarn pull` - Настройка ассетов проекта

### Сборка

- `yarn build` - Сборка production версии
- `yarn build:highmem` - Сборка с увеличенным лимитом памяти (для больших проектов)
- `yarn preview` - Предпросмотр production сборки
- `yarn build:deps` - Сборка зависимостей (components, revolt.js)

### Качество кода

- `yarn lint` - Проверка кода линтером
- `yarn lint:fix` - Автоматическое исправление ошибок линтера
- `yarn format` - Форматирование кода с помощью Prettier
- `yarn format:check` - Проверка форматирования без изменений
- `yarn typecheck` - Проверка типов TypeScript
- `yarn check` - Полная проверка (types + lint + format)

### Запуск

- `yarn start` - Запуск production сервера
- `yarn start:inject` - Запуск с инжекцией скриптов

## 🛠️ Настройка для команды

### Git Hooks

Проект использует [Husky](https://typicode.github.io/husky/) для автоматической проверки кода перед коммитом.

При каждом коммите автоматически запускаются:
- ESLint для проверки кода
- Prettier для форматирования
- Проверка только измененных файлов (lint-staged)

### Форматирование кода

Проект использует:
- **Prettier** для форматирования кода
- **ESLint** для проверки качества кода
- **EditorConfig** для единообразия настроек редактора

**Важно:** Перед коммитом убедитесь, что код отформатирован:
```bash
yarn format
```

Или настройте ваш редактор на автоматическое форматирование при сохранении.

### Настройка редактора

#### VS Code

Установите расширения:
- ESLint
- Prettier
- EditorConfig for VS Code

Добавьте в `.vscode/settings.json`:
```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

#### WebStorm / IntelliJ IDEA

1. Включите поддержку EditorConfig (File → Settings → Editor → Code Style)
2. Настройте Prettier (File → Settings → Languages & Frameworks → JavaScript → Prettier)
3. Включите "Run on save" для Prettier

### Line Endings

Проект использует LF (Unix) line endings для всех текстовых файлов. Это настроено через `.gitattributes`.

Если вы работаете на Windows, Git автоматически конвертирует line endings при коммите.

## 📁 Структура проекта

```
zeelo-frontend/
├── src/                    # Исходный код приложения
│   ├── components/         # React компоненты
│   ├── pages/             # Страницы приложения
│   ├── lib/               # Утилиты и хелперы
│   ├── mobx/              # MobX stores
│   └── styles/            # Глобальные стили
├── external/              # Внешние зависимости
│   ├── components/        # UI компоненты
│   ├── revolt.js/         # API клиент
│   └── lang/              # Локализации
├── public/                # Статические файлы
├── scripts/               # Вспомогательные скрипты
└── dist/                  # Собранные файлы (не в git)
```

## 🔧 Технологии

- **Preact** - Легковесная альтернатива React
- **TypeScript** - Типизированный JavaScript
- **Vite** - Сборщик и dev сервер
- **MobX** - State management
- **Styled Components** - CSS-in-JS
- **ESLint** - Линтер
- **Prettier** - Форматтер

## 🤝 Работа в команде

### Workflow

1. Создайте ветку от `main`/`master`:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения и закоммитьте:
   ```bash
   git add .
   git commit -m "feat: add new feature"
   ```

   Git hooks автоматически проверят код перед коммитом.

3. Запушьте изменения:
   ```bash
   git push origin feature/your-feature-name
   ```

4. Создайте Pull Request

### Commit Message Convention

Используйте понятные сообщения коммитов:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление задач сборки, настроек и т.д.

### Перед коммитом

Убедитесь, что:
- ✅ Код проходит `yarn check`
- ✅ Нет ошибок TypeScript
- ✅ Код отформатирован
- ✅ Нет предупреждений линтера

## 🐛 Troubleshooting

### Проблемы с зависимостями

Если возникают проблемы с зависимостями:
```bash
rm -rf node_modules yarn.lock
yarn install
```

### Проблемы с Git hooks

Если hooks не работают:
```bash
yarn prepare
```

### Проблемы с форматированием

Если Prettier не работает:
```bash
yarn format
```

## 📝 Лицензия

MIT
